// In the root directory of the project
// There can only exist in one main.go file in the project
// The main.go file is the entry point of the application
// So files other than that will exist in the subfolder of the project

package main

import "fmt"

// Here we will be working on
// the mongo connection in go lang
// via the mongoDb native driver only

func main() {

	fmt.Println("MongoDB in go lang")
	fmt.Println("Will be natively only using in the mongo drivers only :-) ")
}
